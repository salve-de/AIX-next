# Rovan プロジェクト固有ルール

この文書は、Rovanの現行実装に適用する補足ルールです。普遍的な開発・安全ルールと、現行の北極星は [`AGENTS.md`](AGENTS.md) を正本とします。過去の方針や会話の記録は、現在の仕様として解釈しません。

## 1. プロダクトの境界

- Rovanは、指定した質問パネル・AI面・モデル・地域・言語・測定時点における回答を確認するサービスです。全ユーザーの会話、AI内部順位、市場全体のシェアは取得しません。
- 会社名・商品名・サービス名の入力では、公開サイト候補を表示し、利用者が対象を確認してからクロール・測定します。
- URLまたは社名だけで開始できるようにしますが、結果の正確性を高めるために必要な対象確認・公開前確認を「ゼロ」と表現しません。
- Rovanは、AIの推薦・掲載順位・問い合わせ・契約・売上の結果を保証しません。

## 2. データと公開の境界

- 実測値には質問、パネル版、AI面、モデル、地域、言語、測定時点、成功・失敗数を紐付けます。
- 失敗・未設定・取得不能な観測は欠損として扱い、成功した観測や引用を架空の値で補いません。
- `marketPosition` や `marketSize` は旧保存データとの互換性のため内部に残る場合がありますが、公開DTOや画面で市場順位として表示しません。
- 公開プロフィールは、参照元で確認できる事実または入力者が確認した内容だけを含む下書きです。未知の住所、料金、資格、実績、レビュー、優位性を推測しません。
- 公開ページ・JSON-LD・Markdownの内容は、画面に見える説明と一致させます。本人確認や公的認証がないページを「公式台帳」「公認推薦」「ランキング」と呼びません。
- 公開プロフィールの作成・公開・停止は明示的な操作を必要とし、顧客サイト、CMS、広告、CRMへ自動書き込みしません。
- Google Mapsの評価・口コミを転載しません。権利者からの訂正・非公開・削除の連絡先を公開ページに用意します。

## 3. Watchと自動化

- Watchは同じ質問パネルを再測定し、候補入り、引用URL、測定完全性などの差分を示します。候補入り率は測定質問に対する観測値であり、顧客シェアではありません。
- 競合サイトの変更は、クロール前後の本文差分と取得時刻を永続化した入力がある場合だけ「変更」として扱います。AI回答に現れた候補の増減を、競合サイトの変更と呼びません。
- Change PackとAutoActionは確認案です。公開反映や顧客サイトの変更は、明示的な承認なしに実行しません。
- 変更前後の単純な差分から因果効果を断定しません。比較条件、欠損、外部要因、反復数を併記します。
- 定期処理のデプロイ先・並列数・コスト・IAMは、対象環境で実測・確認してから運用上の事実として記録します。スクリプトの存在だけで本番稼働や大規模耐性を主張しません。

## 4. UI/UX

- 最初の画面は、対象・何を測るか・次の操作を短く示します。説明のためのカードやバッジを増やしすぎません。
- 数字はラベル、分母、測定条件、測定時点を近接表示し、意味のない大きな数値や順位を主役にしません。
- 比較表は列見出し・行見出し・単位を持つHTML tableを使い、カードは一つの主題と一つの行動に限定します。
- すべての操作可能要素はキーボード、フォーカス表示、名前、状態を持ちます。色だけで成功・失敗を伝えません。
- 設計見本・架空データ・未判定・測定不能を実データと区別して表示します。

## 5. 検証と変更管理

- 変更前に `pwd`、ブランチ、`git status --short --branch` を確認し、既存の変更を上書きしません。
- UI変更は、主要な公開ページ、結果、Watch、公開プロフィール、404、APIのレスポンスを実ブラウザまたはHTTPで確認します。
- 最低限、`npm run lint`、`npm test`、`npm run typecheck`、`npm run build`、`git diff --check` を変更内容に応じて実行します。失敗を成功として報告しません。
- ローカル変更、コミット、GitHub同期、デプロイ、本番設定、ブラウザ確認は別々の事実として報告します。依頼がない限りコミット・push・デプロイを行いません。
