# 2026-09-07 公開準備・Safari実操作記録

## 結論

本番公開・一般販売は未完了。今回の作業は公開前の不具合修正、検証、配備用ファイルの準備であり、全機能が本番利用可能になったという記録ではない。

開始時点: `5335ebe2641cc48f91d5caeee440bad660b6cacc`、`feature/positioning-autopilot`、originと同期・作業ツリーclean。今回の変更は同ブランチの後続コミットに記録する。mainへの統合・本番トラフィックの変更は行っていない。

## 今回変更した内容

| 対象 | 修正と意図 |
| --- | --- |
| 診断開始 | Safariで確認した0%停止を修正。Reactのeffect cleanupで中断した通信をURL一致だけで再開しないガードを除去。完了通知のないストリーム終了もエラーにする。 |
| 診断保存 | AI未設定時は開始前に503。結果保存成功前に完了を通知しない。欠損観測を成功値として補完しない。 |
| AI通信 | タイムアウト時に実リクエストをabort。空回答は失敗。失敗行のモデル名を実設定から記録。 |
| 週次測定 | 分割処理途中の観測を永続化し再開時に再利用。測定確定後の通知失敗で測定を再実行しない。claimは処理開始可能な1件ずつ取得。 |
| 永続化 | productionでDB未設定なのにメモリ保存へ退避して成功扱いにする挙動を禁止。DB通信の時間制限とエラー情報の最小化。 |
| 公開プロフィール | 同じ会社の独立した下書きでURLを衝突させない。Watchからは保存済みの公開URLのみ参照、未公開なら元の診断結果へ誘導。 |
| 公開操作 | 下書きの事実・参照元を表示。管理情報をタブ内に保存して再読み込み時に復元。公開停止ボタンを追加。別タブ・タブ終了後の回復は未実装。 |
| 不正・過剰入力 | プロフィール作成レート制限、文字数制限、課金・データ管理のcredential型検証。 |
| 決済 | メールだけから既存Stripe顧客を割り当てない。既存の回復可能な契約で新規Checkoutを拒否。遅延WebhookはStripeの現在状態から照合し、古い契約の通知で置換しない。並行処理の完全解決は未完了。 |
| データ管理 | 未払い状態の回復可能な契約も削除前に停止。削除・監査記録を単一DBトランザクションにするmigration 011を追加。応答喪失後はtoken hashとメール照合で削除receiptを再取得。 |
| 書き出し範囲 | Watch、元Scan、当該Watchの測定runを単一snapshotで出力。管理tokenを除外。公開プロフィールは所有者が別であり除外・保持することを画面にも明記。 |
| 本番設定表示 | seller/support/setupを動的ページにしてビルド時の未設定値を固定しない。healthは設定確認のみと明示。preflightは秘密値を出さず必須設定とDBテーブル到達性を確認。 |
| 配備準備 | Next standalone Dockerfile、Cloud Build設定、明示的project/SA/Secret Manager指定のCloud Run候補配備スクリプト。誤ったbuilds submitのdockerfile指定を修正。Scheduler起動を15分間隔にしDB期日で週次/再開を判別。 |

2つのサブエージェントがcapacity errorで終了したため、途中差分は主担当が引き継いだ。決済・プライバシー担当は完了。サブエージェントの自己申告だけで公開完了とは判定していない。

## 実際のブラウザー確認

ユーザー指定により、最終確認はmacOSの実Safariをネイティブ操作した。アプリ内ブラウザーだけで代替していない。接続先はローカル `http://127.0.0.1:3000`、公開URLではない。

- ホームの入力欄へ `https://example.com` を入力し診断ボタンを押下。
- 修正前: `/scan?input=...` が進捗0%で停止。修正後: 再読み込みにより「診断に必要なAI接続を準備中」の実際の設定不足エラーと戻る操作を表示。
- 診断レポートの設計見本へ遷移し、判定根拠を展開。固定サンプルの回答と参照元が表示された。
- 設計見本の下書き操作、14日間試用ボタンから `/watch?sample=1` への遷移を確認。実際の測定・課金・公開は行っていない。
- Watchの公開プロフィールリンクから見本ページへ移動。JSONリンクを開きJSON本文が表示されることを確認。
- Safariの戻るボタンでプロフィールへ復帰し、掲載停止の問い合わせリンクから `/support` へ移動。連絡先未設定を確認。
- `/data-rights` で削除対象外のプロフィール・共有診断の説明と入力欄を確認。空欄でJSON取得ボタンを押すとSafariの必須入力検証が動作。実データ削除は行っていない。

これは全ページ・全端末・実API・実決済のE2E合格ではない。Safariの全ページconsole、モバイル実機、実データの下書き復元/公開/停止のブラウザー検証は未完了。

## 検証

- `npm test`: 92件成功、失敗0（テスト用DB/プロバイダーのmock・隔離プロセスを含む）。
- `npm run typecheck`、`npm run lint`、`git diff --check`: 成功。
- `npm run build`: 成功。standalone出力、静的23ページ、設定依存ページの動的出力を確認。Dockerイメージのbuild/run自体は未検証。
- `bash -n scripts/deploy-cloud-run-web.sh scripts/deploy-cloud-run-job.sh`: 成功。実配備コマンドの成功証拠ではない。
- `node --import tsx scripts/release-preflight.ts`: `ready:false`。OPENAI_API_KEY、SUPABASE_URL、SUPABASE_SERVICE_ROLE_KEY、RATE_LIMIT_SALT、NEXT_PUBLIC_SITE_URLが不足。
- `tests/privacy-transaction.sql`: 未実行。使い捨てPostgreSQL初期化が進まず停止。migration 011の実DB適用・rollback・ロック競合は未検証。
- 一部sandbox内のtsx/Nodeテスト起動はIPC/子プロセス制約で失敗。許可済みの実行環境で `npm test` を再実行し上記92件を確認。

## 公開を止めている項目

1. **本番環境の指定・接続**: 確認可能なGCP/Supabase/VercelにはRovan専用環境が見つからない。他プロジェクトのdefaultや秘密を流用していない。新設か既存指定かをユーザーに確認中。
2. **実API・通知・販売設定**: AI3社、Supabase、Stripe、Resend、本番origin、販売者・問い合わせ情報。秘密はGit/チャットに貼らず専用設定先へ保存する必要がある。
3. **決済の未解決箇所**: Checkoutの永続的な重複防止、削除時の未完了Checkout失効、並行Webhookの原子的照合、legacy契約ID欠損の回復。現在の逐次mockテストだけで一般販売しない。
4. **DB実検証**: migrations 001〜011、RLS/role権限、receipt再試行、共有Scan保持、削除時の短いtable lockと測定worker競合、バックアップ復元を確認する。
5. **公開プロフィール管理**: Watchと独立した管理tokenであるため、Watch削除ではプロフィールを削除しない。タブ終了後の無人回復・恒久管理導線は未実装。問い合わせ先も未設定。
6. **実運用証拠**: 実URL診断→DB保存→再読込→公開→停止、定期起動→分割再開→メール、Stripeテスト契約→解約→再送Webhook→削除を通す。費用・負荷・監視・障害復帰を確認後に公開トラフィックを切り替える。

## 参照した一次資料

- [Google Cloud: Next.js on Cloud Run](https://docs.cloud.google.com/run/docs/quickstarts/frameworks/deploy-nextjs-service)
- [Google Cloud: scheduled jobs](https://docs.cloud.google.com/run/docs/execute/jobs-on-schedule?hl=en)
- [Google Cloud: Secret Manager integration](https://docs.cloud.google.com/run/docs/configuring/services/secrets)
- [gcloud builds submit](https://docs.cloud.google.com/sdk/gcloud/reference/builds/submit)
- [Supabase production checklist](https://supabase.com/docs/guides/deployment/going-into-prod)
- [Supabase migrations](https://supabase.com/docs/guides/deployment/database-migrations)
- [Vercel Hobby plan](https://vercel.com/docs/plans/hobby): 確認可能なアカウントはHobby。商用向け公開先として採用していない。
- [Node 22.23.2 security release](https://nodejs.org/en/blog/release/v22.23.2): コンテナのNodeを固定。
- [OpenAI GPT-5.6 Luna](https://developers.openai.com/api/docs/models/gpt-5.6-luna)、[Gemini 3.6 Flash](https://ai.google.dev/gemini-api/docs/models/gemini-3.6-flash): 現設定のモデル名・検索対応を確認。実アカウントの利用権限や実応答は未確認。
- インストール済みNext 16.3.4の `node_modules/next/dist/docs/` のdeploying / route handlers / output説明、React実装確認スキル。standalone配置、設定ページの動的化、effectの中断と再開を確認に使用。
